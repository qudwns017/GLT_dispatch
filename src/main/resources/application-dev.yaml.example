spring:
  datasource:
    url: jdbc:mysql://{dbURL}?serverTimezone=UTC&characterEncoding=UTF-8
    username: {id}
    password: {pw}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100